import { Routes } from "react-router-dom";
import salesRoute from "./sales.route";

const AppRoutes = () => {
  return (
    <Routes>
      {salesRoute}
    </Routes>
  );
};

export default AppRoutes;
